/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Standalone output so App Engine can run the Next.js server
  output: "standalone"
};

export default nextConfig;
